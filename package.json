{
    "name": "aws-bootstrap",
    "version": "1.0.0",
    "description": "Bootstrap for AWS",
    "main": "server.js",
    "scripts": {
        "start": "node ./node_modules/pm2/bin/pm2 start server.js --name \"aws-bootstrap\" --watch --log ../logs/aws-bootstrap.log",
        "stop": "node ./node_modules/pm2/bin/pm2 stop server.js --name \"aws-bootstrap\"",
        "build": "echo \"No build supported yet\" && exit 0"

    },
    "dependencies": {
        "pm2": "^5.3.0"
    }
}
